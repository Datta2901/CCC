#include<bits/stdc++.h>
using namespace std;

bool isVowel(char a){
    if(a == 'a' || a == 'e' || a == 'i' || a == 'o' || a == 'u'|| a == 'y' ){
        return true;
    }
    return false;
}

int main(){
    string name;
    cin >> name;
    for(int i = 0; i < name.size(); i++){

    }
    return 0;
}