#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin >> n;
    vector<int>cost,number;
    for(int i = 0; i < n; i++){
        int a,b;
        cin >> a >> b;
        number.push_back(a);
        cost.push_back(b);
    }
    int i = 0;
    while(i < n){
        if
    }
    return 0;
}