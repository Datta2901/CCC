#include<bits/stdc++.h>
using namespace std;
int main(){
    int size,times;
    cin >> size >> times;
    vector<int>numbers;
    for(int i = 0; i < size; i++){
        int a;
        cin >> a;
        numbers.push_back(a);
    }
    times = size % times;
    for(int i = times)
    return 0;
}